---

description: Run the full skat-ai project check
agent: build
------------

Run the full project check:

```powershell
.\scripts\check.ps1
```

If it fails:

1. summarize the failing step
2. identify the most likely cause
3. propose the smallest fix
4. do not make unrelated changes
