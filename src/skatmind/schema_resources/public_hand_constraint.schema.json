{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/skatmind/public_hand_constraint.schema.json",
  "title": "skatmind public hand constraint output",
  "type": "object",
  "additionalProperties": false,
  "required": ["player", "source", "visibility_scope", "card_count", "cards"],
  "properties": {
    "player": {
      "type": "string",
      "enum": ["me", "left", "right"]
    },
    "source": {
      "type": "string",
      "enum": [
        "declared_ouvert",
        "declarer_card_exposure_continuation",
        "defender_open_play_continuation"
      ]
    },
    "visibility_scope": {"const": "all_players"},
    "card_count": {
      "type": "integer",
      "minimum": 0,
      "maximum": 10
    },
    "cards": {
      "type": "array",
      "maxItems": 10,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[CSHD](A|10|K|Q|J|9|8|7)$"
      }
    }
  }
}
